<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/locationadd"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<TextView
    android:id="@+id/newl"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:text="Looks like you are on new location"
    android:textColor="@color/orange"
    android:textSize="20sp"
    android:padding="10sp"
    app:layout_constraintEnd_toEndOf="parent"
    android:fontFamily="@font/avenir_light"
    android:textAlignment="center"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    />
    <com.google.android.material.button.MaterialButton
        android:id="@+id/addAddress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/selectadd"
        app:layout_constraintTop_toBottomOf="@+id/newl"
        />
    <com.google.android.material.button.MaterialButton
        android:id="@+id/selectadd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/addAddress"
        app:layout_constraintTop_toBottomOf="@+id/newl"
        android:layout_marginStart="30dp"

        />

</androidx.constraintlayout.widget.ConstraintLayout>